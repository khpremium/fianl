<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="res">

	<select id="dList" parameterType="reservation.ReservationDTO" resultType="reservation.AirinfoDTO">
		select * from airinfo
		where d_time like #{dep_date} || '%'
		and city_code_dep=#{city_code_dep}
		and city_code_arv=#{city_code_arv}
	</select>

	<select id="rList" parameterType="reservation.ReservationDTO" resultType="reservation.AirinfoDTO">
		select * from airinfo
		where d_time like #{arv_date} || '%'
		and city_code_dep=#{city_code_arv}
		and city_code_arv=#{city_code_dep}
	</select>

	<select id="name" parameterType="string" resultType="string">
		select city_name from city where city_code=#{city_code}
	</select>

	<update id="seatUpt" parameterType="string">
		update airinfo set
		seat=seat-1 where flight=#{flight}
	</update>

	<!-- #{data, jdbcType=VARCHAR} null처리 -->
	<insert id="client" parameterType="reservation.ReservationDTO">
		insert into
		reservation(rv_num, rv_code, payment_chk, airinfo_flight, user_id, p_count)
		values(rv_num_seq.nextval, #{rv_code}, #{payment_chk}, #{airinfo_flight}, #{user_id}, #{p_count})
	</insert>

	<insert id="guest" parameterType="reservation.ReservationDTO">
		insert into
		reservation(rv_num, rv_code,
		payment_chk, airinfo_flight, non_name, non_gender, non_phonenumber,
		non_email, non_pass, p_count)
		values(rv_num_seq.nextval, #{rv_code}, #{payment_chk},
		#{airinfo_flight}, #{non_name}, #{non_gender}, #{non_phonenumber},
		#{non_email}, #{non_pass}, #{p_count})
	</insert>
	
	<select id="rvchk" parameterType="string" resultType="int">
		select count(*) from reservation where rv_code=#{rv_code}
	</select>
	
	<select id="price" parameterType="string" resultType="int">
		select price_ad from airinfo where flight=#{flight} and rownum=1
	</select>
	
	<update id="point" parameterType="java.util.HashMap">
		update client set point=point+#{point} where id=#{id}
	</update>

</mapper>